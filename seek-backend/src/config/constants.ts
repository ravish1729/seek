const service = 'ipfsX'

const jwtAlgo = 'HS256'
const jwtExpire = '100d'

const messageString = 'Please prove ownership this wallet by signing this nonce, nonce='
const tagLimitPerCID = 15

export {
  service,
  jwtAlgo,
  jwtExpire,
  messageString,
  tagLimitPerCID
}
